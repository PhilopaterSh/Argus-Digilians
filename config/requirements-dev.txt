# Development / CI dependencies (specs/012-spec-reconciliation section 7).
# Runtime dependencies are in config/requirements.txt.
ruff
mypy
pytest
pytest-cov

# benchmarks/fixtures/ssti_template_injection (specs/025) - already present transitively via
# langchain, pinned explicitly since the benchmark suite imports it directly.
jinja2
